# 2026-05-22

## Pre-compaction memory flush (00:41 BRT)

### Encoding bug — FIXED
- **File:** `adapter-utils/dist/server-utils.js` L1532, L1548
- **Root cause:** `String(chunk)` on Windows uses CP850, not UTF-8
- **Fix:** `typeof chunk === "string" ? chunk : chunk.toString("utf8")` in stdout & stderr data handlers
- Still needs Paperclip restart to reload modules from npx cache

### Waste cleanup
- Cancelled 2 "Review silent active run" issues (CTO, Esp. Automotivo) — meta-overhead puro
- Marked "Preparar ambiente de integração" (RLA-6) and "Criar dataset 50 peças" (RLA-4) as done
- Modelo do CTO corrigido: `openai/gpt-5.1-codex-mini` (inexistente) → `opencode/big-pickle`
- All 5 blocked issues resolved → 0 blocked

### Environment
- NVIDIA_API_KEY e OPENROUTER_API_KEY copiados do `~/.openclaw/.env` para User env vars do Windows
- Agents Paperclip acessam direto via `process.env` agora

### Perplexity Pro Bridge skill created
- `skills/perplexity-pro-bridge/SKILL.md` — workflow completo para browser tool
- Modelos mapeados: Sonar 2, GPT-5.4, GPT-5.5 Max, Gemini 3.1 Pro, Claude Sonnet 4.6, Claude Opus 4.7 Max, Kimi K2.6, Nemotron 3 Super
- Browser tool 100% funcional no profile `openclaw`

### Estado final dos agents
- 6 active (idle/paused), 0 running, 0 error, 0 paused
- 12 tasks done, 0 blocked, 0 inProgress, 0 open
- CEO aguardando restart do Paperclip para aplicar encoding fix

### Learned: Eng. Percepção rule
- Nemotron "omni" (non-reasoning) sempre para visão. Nunca reasoning.
- Se Nemotron bloqueado, não auto-fallback para gpt-4o-mini (content filter muito agressivo). Aguardar instruções.
